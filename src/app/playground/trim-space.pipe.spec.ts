import { TrimSpacePipe } from './trim-space.pipe';

describe('TrimSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new TrimSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
