import { LogOnClickDirective } from './log-on-click.directive';

describe('LogOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new LogOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
